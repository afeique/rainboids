//! Orbs: spawn, attract, collect. Stub.

use crate::protocol::{DropState, GameEvent, ShipState};

pub fn update(
    _drops: &mut Vec<DropState>,
    _ships: &[ShipState],
    _dt: f32,
    _events: &mut Vec<GameEvent>,
) {
}
